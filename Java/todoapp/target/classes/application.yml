spring:
  datasource:
    hikari:
      jdbc-url: jdbc:postgresql://localhost:5432/todoapp
      username: postgres
      password: Kappa123
      driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
  mvc:
    view:
      prefix: /
      suffix: .jsp
  messages:
    basename: validation
logging:
  level:
    root: WARN
    com.kenzo47.todoapp.authentication: DEBUG